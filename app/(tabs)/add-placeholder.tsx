// This is a placeholder screen that never renders
// It exists to create space in the tab bar for the FAB
import { View } from "react-native";

export default function AddPlaceholder() {
  return <View />;
}
